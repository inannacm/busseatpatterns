
  <div class="cards">

<% @patterns.each do |pattern| %>
<%= link_to pattern_path(pattern) do %>
<% if pattern.photo.attached? %>
<div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.001), rgba(0,0,0,0.001)), url('<%= cl_image_path pattern.photo.key, crop: :fill %>');">

 <% end %>



    </div>
    <% end %>
  <% end %>
  </div>
